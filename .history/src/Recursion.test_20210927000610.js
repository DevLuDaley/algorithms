// function strCount(str, letter){
const { factorialI } = require('./Recursion');
// const twoHighest = require('./twoHighest');

test('first', () => {
  expect(factorialI([5])).toEqual([121]);
  // expect(strCount('lllcdc', 'k')).toBe(0);
});
