function arrayDiff(a, b) {
  for (let i = 0; i < a.length; i++) {
    i;
    const element = a[i];
for (let n = 0; n < b.length; n++) {
    const element = b[n];
    b[n] == a[i]? a.replace(a[i],"")
}  b[n] == a[i]? a.replace(a[i],"")

}
}

arrayDiff([3, 4], [3]);
