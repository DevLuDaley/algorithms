function arrayDiff(a, b) {
  const returnThis = [];
  for (let i = 0; i < a.length; i++) {
    i;
    const element = a[i];
    for (let n = 0; n < b.length; n++) {
      const element = b[n];
      b[n] != a[i] ? a.replace(a[i], '') : null;
    } // b[n] == a[i]? a.replace(a[i],"")
  }
}

arrayDiff([3, 4], [3]);
